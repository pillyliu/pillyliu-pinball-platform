# Project TODO

- Keep `lpl-targets` as plain static `index.html` for now.
- Future: migrate `lpl-targets` into a Vite/React project.
- Future: wire migrated `lpl-targets` into the shared pinball data cache system (hash manifest, selective refresh, offline support) used by other pages/app.
